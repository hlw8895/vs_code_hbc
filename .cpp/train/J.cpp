#include<iostream>
#include<algorithm>
#include<vector>
#include<array>
using namespace std;
#define endl '\n'
static auto speedup = []() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return nullptr;
}();
int main()
{
    int n, m;
    cin >> n, m;
    array<int, 20> arr = {};
    for (int i = 0; i < m;i++)
        cin >> arr[i];
    
        return 0;
}
